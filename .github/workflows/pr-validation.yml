name: Validate pull request before putting into queue

on:
  pull_request:
    branches:
      - main

jobs:
  validate-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run fast CI (e.g., linting, tests, etc.)
        run: |
          echo "Running fast validation..."
          sleep 5
          echo "Fast validation complete"
